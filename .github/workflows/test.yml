name: Test workflow

# Trigger on version tags only
on:
  push:
    tags:
      - "*"

jobs:
  build-packages:
    name: Test
    runs-on: ubuntu-latest

    steps:
      - name: Echo test
        run: |
          echo "Test"
